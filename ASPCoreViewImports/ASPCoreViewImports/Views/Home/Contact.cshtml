@model List<Students>

@{
    ViewData["Title"] = "Contact";
}

<h1>Contact</h1>
@{
    foreach (var item in Model)
    {

        <h2>@item.Id</h2>
        <h2>@item.Name</h2>
        <h2>@item.Gender</h2>

    }

}
